import React from "react";

export default function Header() {
  return (
    <header
      style={{
        background: "#095aa7",
        borderRadius: "25px",
        color: "#fff",
        textAlign: "center",
        marginBottom: "10px",
        fontFamily: "Arial"
      }}
    >
      <h1>Roster Browser</h1>
    </header>
  );
}
